import 'package:flutter/material.dart';

class AddNewsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add News'),
      ),
      body: Center(
        child: Text('Add News Page'),
      ),
    );
  }
}
